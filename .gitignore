__pycache__/
*.pyc
.DS_Store
logs/*.log
keys.local.json
# local/ = this Mac only (3D village, scratch). Never committed, never served.
/local/
# .github/workflows/farm.yml needs a workflow-scope token to push — unignore when granted
.github/workflows/farm.yml
.env*
*-key*.txt
*-key*.json
